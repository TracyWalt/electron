module.exports = {
    config: {
        // 默认配置文件存储盘符
        'cfgRootPath': 'C:electron-cmd',
        // 默认配置文件存储路径
        'cfgPath': 'C:/electron-cmd/config.json',
        // 默认读取的路径
        'rootDir': 'E:/'
    }
}